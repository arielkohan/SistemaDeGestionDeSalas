/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OneHourAfterTimeDirective } from './one-hour-after-time.directive';

describe('OneHourAfterTimeDirective', () => {
  it('should create an instance', () => {
    let directive = new OneHourAfterTimeDirective();
    expect(directive).toBeTruthy();
  });
});
