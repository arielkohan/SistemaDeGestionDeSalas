/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MinDateTodayValidatorDirective } from './min-date-today.directive';

describe('MinDateTodayValidatorDirective', () => {
  it('should create an instance', () => {
    let directive = new MinDateTodayDirective();
    expect(directive).toBeTruthy();
  });
});
